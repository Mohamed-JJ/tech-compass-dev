/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow images from public directory (which we'll use for mockups)
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
